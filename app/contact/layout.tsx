export const metadata = {
  title: "Contact | wURLd Web Design",
  description:
    "Got a project in mind? Get in touch for a custom coded website built from scratch.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
