export const metadata = {
  title: "Portfolio | wURLd Web Design",
  description:
    "Custom coded websites built from scratch. View previous work including photography sites, consulting pages, and more.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
